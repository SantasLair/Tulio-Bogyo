// NewScript
// Lifecycle hooks for game objects

function create() {
  // Called when the object is created
  console.log('NewScript created');
}

function update(delta) {
  // Called every frame
  // delta: time since last frame in milliseconds
}

function destroy() {
  // Called when the object is destroyed
}
